## 1. Shared page chrome

- [x] 1.1 Create `src/components/crud/resource-form-layout.tsx` by extracting the non-form half of `resource-form-page.tsx`: the `PageHeader` with its Cancel / "Save and continue editing" / "Save and return" buttons, the dismissible error `Alert` above the fields, the `Card` wrapper, the loading spinner, and the load-failure state with "Back to list". Props: `noun`, `listPath`, `isEdit`, `saving`, `error`, `onDismissError`, `onCancel`, `onSubmit(andReturn: boolean)`, `isLoading`, `loadError`, `title?`, `description?`, `footer?`, `children`.
- [x] 1.2 Rewrite `src/components/crud/resource-form-page.tsx` to compose `ResourceFormLayout`, keeping its exported props and behaviour byte-for-byte identical — the antd `Form`, `form.submit()`/`onFinish`, the `returnAfterSave` ref, the create→edit `navigate(..., { replace: true })`, and the `record`-keyed `setFieldsValue` effect.
- [x] 1.3 Confirm `src/components/crud/resource-form-page.test.tsx` still passes unchanged. If a test reaches into internals that moved, adjust the test, not the behaviour.
- [x] 1.4 Create `src/components/crud/resource-form-page-rhf.tsx`: same public contract as `ResourceFormPage` (`noun`, `listPath`, `recordId`, `record`, `isLoading`, `loadError`, `onSave`, `title?`, `description?`, `footer?`) but taking a caller-supplied `UseFormReturn` and rendering `children` inside the shadcn `Form` provider. It must (a) `form.reset(toValues(record))` in an effect keyed on the record so switching records re-syncs, (b) route validation failures to the layout's error slot with "Some fields need attention…", (c) leave all entered values in place when `onSave` rejects and show the thrown message above the fields, (d) do the same create→edit `replace` navigation on "Save and continue editing".
- [x] 1.5 Add a test for `ResourceFormPageRHF` mirroring `resource-form-page.test.tsx`: a rejected save keeps the typed values and shows the reason; "Save and return" navigates to `listPath`; "Save and continue editing" on a create navigates to `${listPath}/${id}` with replace.

### Single-record reads (added during apply, approved by the user)

An overlay was handed the row object it was opened from, so four resources never needed a single-record read. A deep-linked edit page does. The backend exposes "Get by id" for all four; the hook pattern already exists verbatim in `banners.ts`, `coupons.ts`, `brands.ts`, `categories.ts`, `campaigns.ts` and `roles.ts`. This adds new exports only — no existing function, signature or query key changes.

- [x] 1.6 `src/lib/api/suppliers.ts`: add `getSupplier(id)` calling `GET /suppliers/:id` and `useSupplier(id?)` keyed on the existing `queryKeys.suppliers.detail`, `enabled: !!id`.
- [x] 1.7 `src/lib/api/warehouses.ts`: same, on `GET /warehouses/:id` and the existing `queryKeys.warehouses.detail`.
- [x] 1.8 `src/lib/api/shipping-methods.ts`: same, on `GET /shipping-methods/:id`; add a `detail` key to `queryKeys.shippingMethods` first.
- [x] 1.9 `src/lib/api/staff-users.ts`: same, on `GET /users/:id`; add a `detail` key to `queryKeys.staffUsers` first.

## 2. Suppliers — the first conversion, reviewed before the rest

- [x] 2.1 Export `SUPPLIERS_PATH = '/inventory/suppliers'` from `src/features/inventory/suppliers/suppliers-page.tsx`.
- [x] 2.2 Create `src/features/inventory/suppliers/supplier-form-page.tsx` on `ResourceFormPageRHF`, moving the sheet's zod schema, `useForm` call, and field JSX from `suppliers-page.tsx` verbatim; read `supplierId` from `useParams`, load via the existing supplier hook, and save via the existing create/update mutations.
- [x] 2.3 Add lazy import and routes `/inventory/suppliers/new` and `/inventory/suppliers/:supplierId` in `src/routes/app-router.tsx`, both pointing at `SupplierFormPage`, with the same "one component serves both" comment as the existing pairs.
- [x] 2.4 Strip the `Sheet` block, `sheetOpen`/`editing` state, form imports and mutation wiring from `suppliers-page.tsx`; make the create button and the row Edit item `navigate()` instead. Leave `ConfirmDialog` and the delete flow untouched.
- [x] 2.5 Verify against the spec: create from the list, edit from a row, open `/inventory/suppliers/:id` in a fresh tab, reload a create page, press back, cancel, save-and-return, save-and-continue on a create, and a rejected save keeping the form. Review this conversion before starting section 3.

## 3. Remaining react-hook-form conversions

Each item follows the same five steps as section 2 — export the path constant, move schema + `useForm` + fields into a new form page on `ResourceFormPageRHF`, add the routes, strip the overlay from the list page, verify. One commit per resource.

- [x] 3.1 Warehouses → `warehouse-form-page.tsx`, routes `/inventory/warehouses/new` and `/inventory/warehouses/:warehouseId`.
- [x] 3.2 Shipping methods → `shipping-method-form-page.tsx`, routes `/sales/shipping-methods/new` and `/sales/shipping-methods/:shippingMethodId`. Preserve the diverging `Values`/`OutputValues` generics the sheet uses.
- [x] 3.3 Vouchers → `voucher-form-page.tsx`, routes `/marketing/vouchers/new` and `/marketing/vouchers/:voucherId`. Preserve the `Values`/`OutputValues` generics.
- [x] 3.4 Banners → `banner-form-page.tsx`, routes `/marketing/banners/new` and `/marketing/banners/:bannerId`. This is the largest form (~150 lines of fields, plus the `openSheet`/`closeSheet` reset behaviour and the generics note at `banners-page.tsx:56`); give the fields the extra width the page affords without changing the fields themselves.
- [x] 3.5 Campaigns → `campaign-form-page.tsx`, route `/marketing/campaigns/new` only. Keep the existing post-create navigation to `/marketing/campaigns/:id`. Do not add an edit route — campaign editing already happens in place on the detail page.
- [x] 3.6 Roles → `role-form-page.tsx`, routes `/settings/roles/new` and `/settings/roles/:roleId`, both declared inside the existing `RoleGuard roles={['OWNER']}` element. The edit page authors name and description via `useUpdateRole` — a surface the admin never exposed, added during apply and approved by the user, because a create page whose "Save and continue editing" has nowhere to land would either lie or create duplicate roles. Permission editing stays inline on `roles-permissions-page.tsx`; the list gains an Edit action per role.
- [x] 3.7 Staff users → `staff-user-form-page.tsx`, route `/settings/staff/:userId` only, declared inside the existing `RoleGuard roles={['OWNER','ADMIN']}` element. Preserve the OWNER-only role-change guard the dialog applies before patching.

## 4. Catalog conversions (antd forms)

- [x] 4.1 Brands: create `brand-form-page.tsx` on the antd `ResourceFormPage`, moving the fields from `BrandCreateModal` and `BrandEditModal` into one component; routes `/catalog/brands/new` and `/catalog/brands/:brandId`.
- [x] 4.2 Brands bulk: create `brand-bulk-create-page.tsx` from `BrandBulkCreateModal` on `ResourceFormLayout` directly (single Save, no create→edit navigation, returns to the brands list on success); route `/catalog/brands/bulk`.
- [x] 4.3 Delete `src/features/catalog/brands/brand-form-modal.tsx` and strip the modal state, imports and triggers from `brands-page.tsx`; the create, bulk and row-edit actions become navigations.
- [x] 4.4 Categories: create `category-form-page.tsx` on `ResourceFormPage`, merging `CategoryCreateModal` and `CategoryEditModal`; keep `category-parent-picker.tsx` as the parent field. Read the pre-selected parent from `?parentId=` via `useSearchParams` (design Decision 4). Routes `/catalog/categories/new` and `/catalog/categories/:categoryId`.
- [x] 4.5 Add routes `/catalog/sub-categories/new` and `/catalog/sub-categories/:categoryId` pointing at the same `CategoryFormPage`; have the page derive its `listPath` from which route matched, so Cancel and "Save and return" land on the list the merchant came from (design Decision 5).
- [x] 4.6 Delete `src/features/catalog/categories/category-form-modal.tsx`; strip the modal state and imports from `categories-page.tsx` and `sub-categories-page.tsx`. In the tree, the per-node "add child" action navigates to `/catalog/categories/new?parentId=<node id>`; in sub-categories, create navigates with the currently scoped parent applied the same way.

## 5. Routing and access control

- [x] 5.1 Review `src/routes/app-router.tsx` as a whole: every new `/new` and `/:id` pair renders the same component, every new route is `lazy()`, and the settings routes sit inside their correct `RoleGuard`.
- [x] 5.2 Confirm no new route shadows an existing one — in particular that `/catalog/brands/bulk` and `/catalog/brands/new` are declared before `/catalog/brands/:brandId`, and that `/inventory/suppliers/new` is not swallowed by `/inventory/suppliers/:supplierId`.
- [x] 5.3 Confirm `src/routes/nav-config.ts` is unchanged and no authoring page appears in the sidebar.

## 6. Verification

- [x] 6.1 Grep `src/features/` for `SheetContent`, `DialogContent` and antd `Modal`: the only remaining hits must be the exempt surfaces named in the spec — reviews reply, purchase-order receive items, stock adjust, order status / record payment / shipment, refunds issue + detail, returns complete + set status, audit-log detail — plus `ConfirmDialog`/`ReassignDeleteDialog` usage and the variant-editor's "combinations cannot be carried over" warning. (That last one is an antd `Modal`; the original wording of this task said no antd `Modal` may remain, which was written from an incomplete survey — it is a destructive confirmation, which the spec exempts, not record authoring.) No authoring overlay remains.
- [ ] 6.2 Walk every converted resource against `specs/admin-shell/spec.md`: deep-link an edit URL in a fresh tab, reload a create page, browser-back to the list, cancel, both save buttons, an unknown record id, and a rejected save.
- [x] 6.3 Check record-switching: edit record A, go back, edit record B — B's values must render, not A's. Covered by two tests in `resource-form-page-rhf.test.tsx`. Implementing this surfaced a second case the plan had not anticipated: `/:id` and `/new` are deliberately one component, so going straight from an edit URL to the create URL left the edited record in the fields, and saving would have created a copy of it. Both shells now empty the form when the record id goes away; the react-hook-form one needs `reset(values, { keepDefaultValues: true })` when syncing a record, or react-hook-form adopts that record as the form's defaults and the later bare `reset()` restores it.
- [ ] 6.4 As a non-ADMIN account, open `/settings/staff/:userId` and `/settings/roles/new` directly and confirm both are refused exactly as their list pages are. **Needs a running app and backend — not done.** Statically: both routes are declared inside the same `RoleGuard` elements as their list pages (`app-router.tsx:203-213`), which is the mechanism the requirement rests on.
- [ ] 6.5 Confirm the category parent pre-fill: add-child from a tree node opens the create page with that parent set, the plain create opens with none, and reloading the pre-filled page keeps the parent. **Needs a running app and backend — not done.** Statically: the tree's add-child navigates to `?parentId=<id>`, sub-categories' create does the same with the browsed parent, and `category-form-page.tsx` reads it via `useSearchParams` into `emptyValues.parentId`.
- [ ] 6.6 Run `pnpm lint`, `pnpm test`, and `pnpm build`; confirm no unused imports or dead state remain in the eleven list pages. **Two of three pass.** `pnpm lint` — 0 errors (4 warnings, all the pre-existing react-compiler "incompatible library" notice on `form.watch`/`useReactTable`); `no-unused-vars` clean, so no dead imports or state remain. `pnpm test` — 27/27. `pnpm build` **fails, for a reason that predates this change and is unrelated to it**: `pnpm build` is `tsc -b && vite build`, and `tsc -b` rejects `vite.config.ts:14` because `defineConfig` is imported from `vite` rather than `vitest/config`, so the `test` key is not in its type. Nothing under `src/` affects it. `tsc -p tsconfig.app.json` (all app sources) and `vite build` both pass on their own. Left alone as out of scope; the fix is a one-line import swap.
