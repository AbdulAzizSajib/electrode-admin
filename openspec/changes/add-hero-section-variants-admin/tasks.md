## 1. Types and registry mirror

- [x] 1.1 In `src/lib/api/store-settings.ts`, add `HeroVariant = 'SPLIT_THREE' | 'SPLIT_ONE' | 'FULL_SLIDER' | 'SLIDER_STACK'` mirroring the server's `HERO_VARIANTS` tuple, in the same order — position 0 is the default.
- [x] 1.2 Add `HERO_VARIANT_OPTIONS`: per variant, a merchant-facing `label` and a one-line `description` of the arrangement. No internal names reach the screen.
- [x] 1.3 Add optional `variant?: HeroVariant` to the `HomeConfig` entry type.
- [x] 1.4 Leave `DEFAULT_HOME_CONFIG` variant-free, matching the server (design.md Decision 6). Comment why.
- [x] 1.5 Rewrite `HOME_SECTION_REGISTRY`'s `HERO` description — it currently reads "with the slider and its side tiles", which describes one arrangement as though it were the only one.

## 2. Per-layout geometry

- [x] 2.1 In `src/features/ui/home-slider/hero-slots.ts`, add `HERO_GEOMETRY: Record<HeroVariant, …>` holding each arrangement's column fraction, tile count, tile ratios and slider ratio, using the figures in design.md Decision 7. Keep `SPLIT_THREE`'s numbers **exactly** as `SIDE_COLUMN_FRACTION` and `PROMO_RATIO` hold them today.
- [x] 2.2 Change `renderedSize` to `renderedSize(variant, placement, contentWidth)`, moving the current arithmetic under the `SPLIT_THREE` case and adding the other three. Everything stays a fraction or a ratio; `HERO_GAP`, `CONTENT_PADDING`, `WIDEST_CONTENT_WIDTH` and `FULL_WIDTH_REFERENCE` are unchanged.
- [x] 2.3 Replace the `HERO_SLOTS` constant with `heroSlots(variant): readonly HeroSlot[]`, returning only the slots that arrangement renders, with per-arrangement `capacity`, `label` and `description`. `recommended` keeps deriving from `widest`/`forUpload`, so it follows automatically.
- [x] 2.4 Update `getHeroSlot` to take the variant. Leave `isHeroPlacement`, `HERO_PLACEMENTS`, `formatSize`, `formatRatio`, `RATIO_TOLERANCE`, `MOBILE_ARTWORK` and `checkDimensions` alone — `checkDimensions` reads `slot.recommended` and needs no change.
- [x] 2.5 Add `unusedSlots(variant)` returning the hero placements that arrangement does not render, for the group in section 4.
- [x] 2.6 Rewrite the module's header comment: it currently explains the one layout's pixels-to-ratios history. Keep that history and add that the geometry is now per arrangement and must match `frontend/src/components/home/hero/`.
- [x] 2.7 Add `hero-slots.test.ts`: `SPLIT_THREE`'s `renderedSize` and `recommended` match the values the module produces today at widths 1140/1440/1600/full (a regression guard on the refactor); each arrangement's slot list has the right placements and capacities; `unusedSlots` is the complement.

## 3. The layout picker

- [x] 3.1 Add `variant-picker.tsx` under `src/features/ui/home-slider/`: a radio group of four cards, each with an inline SVG diagram of the arrangement at its real ratios, plus label and description. **Not a Radix `Select`** — design.md, Risks.
- [x] 3.2 Mount it on `home-slider-page.tsx` above the slot grid, selected from the settings payload. Do not default it in the panel; the server always resolves it (design.md Decision 6).
- [x] 3.3 Keyboard and screen-reader support: a labelled radio group, arrow-key navigation between options, a visible focus ring on the card. If a focusable `sr-only` element is used, give its parent `relative` — otherwise focusing it scrolls the fixed admin shell off screen.
- [x] 3.4 Saving: refetch `useStoreSettings` first, build `homeConfig` from **that** response, change only the `HERO` entry's `variant`, and `PATCH` the whole array (design.md Decision 2). Send `homeConfig` only — no other editor's fields.
- [x] 3.5 Success and failure toasts matching the page's existing ones; a refused save leaves the previous selection shown, with the backend's own message above the picker.

## 4. Layout-aware slot management

- [x] 4.1 Drive the slot grid from `heroSlots(variant)` so it is laid out as the chosen arrangement lays it out — one wide box for `FULL_SLIDER`, a wide box above a row of three for `SLIDER_STACK`.
- [x] 4.2 Pass the current arrangement's slot into `slot-editor-dialog.tsx` so the size guidance beside the upload control is that arrangement's. Confirm `checkDimensions` now warns against the right shape.
- [x] 4.3 Add the **"Not used by this layout"** group below the grid, listing any slot from `unusedSlots(variant)` that still holds banners, fully editable, headed so that "kept — used again if you switch back" is the first thing read (design.md Decision 4). Omit the group entirely when it is empty.
- [x] 4.4 Recount the existing capacity-overflow message against the current arrangement's capacity. The wording already exists; only the number changes.
- [x] 4.5 Update `PAGE_DESCRIPTION` — "laid out as the storefront renders it" is still true, and now true of four arrangements, so say which one is in view.

## 5. Home Sections cross-reference

- [x] 5.1 On `home-sections-page.tsx`, add a read-only line to the `HERO` row naming the current arrangement, linking to Home Slider. No control — one place makes the choice (spec: the section list points at where the arrangement is set).
- [x] 5.2 Fix the normalisation around line 380: it rebuilds entries as `{ key, enabled }` and would **drop a stored `variant` on every save from this page**. Carry the field through. This is the same silent-drop hazard the server change calls out in its reconciler, and it is the most likely defect in this change.
- [x] 5.3 Add a test asserting that saving from Home Sections preserves an existing `HERO` variant.

## 6. Verification

- [x] 6.1 `cd admin && npx vitest run src/features/ui/home-slider/hero-slots.test.ts` (flags do not survive `npm --prefix`), on an uppercase `E:\` path.
- [x] 6.2 `npm run lint` and `npx tsc --noEmit` clean. Confirm no call site of `renderedSize` or `heroSlots` was left without a variant — the signature change exists to force that.
- [x] 6.3 Confirm nothing imports `antd`.
- [ ] 6.4 End to end against a running server and storefront. **Data path verified:** each arrangement saves through `PATCH /settings`, and the storefront then renders exactly the structure that arrangement's diagram shows — the 43% column for the two split layouts, `lg:aspect-3/1` for the two wide ones, `lg:grid-cols-3` only for SLIDER_STACK, and no pixel-sized box in any of them. **Clicking the picker in a browser still needs a human; there is no browser automation in this repo.**
- [x] 6.5 Ratio warning across layouts. `hero-slots.test.ts` asserts it directly: artwork cut for SPLIT_THREE's promo tile, checked against FULL_SLIDER's slider, returns a `ratio` warning whose message says the image will be cropped and names the recommended size — while artwork cut for the layout in use returns null. It stays advisory: a return value, not a guard.
- [ ] 6.6 The "Not used by this layout" group on screen. **Verified below the UI:** `unusedSlots(variant)` is exactly the complement of `heroSlots(variant)` for all four arrangements, the page groups banners by every hero placement rather than only the rendered ones, and cycling every layout through the API leaves every banner byte-identical. **Seeing the group render, and its rows still being editable, needs a human with a browser.**
- [x] 6.7 The HERO variant survives a save from Home Sections. `hero-variant-preserved.test.tsx` drives the real page: toggling an unrelated section, and toggling the hero itself, both send the variant back unchanged, and a store that never chose one still sends none.
- [x] 6.8 Content width. `hero-slots.test.ts` asserts both halves for all four arrangements: `recommended` is identical at 1140/1440/1600/full, and `renderedSize` grows in both dimensions from 1140 to 1600. Every slot also keeps its ratio across all four widths.

## 8. Follow-up: stated upload sizes and a live picker

Raised after the picker shipped — the sizes it quoted were unusable and the
control read as inert on screen.

- [x] 8.1 `HERO_GEOMETRY[variant].upload` states a ROUND pixel size per slot per layout, replacing `recommended = 2x widest()`. The derivation was correct and unusable: 1720×1290, 644×644, 1320×614, 1002×752. `forUpload` now reads the stated table.
- [x] 8.2 Four invariants asserted in `hero-slots.test.ts` for every slot of every layout, replacing what the arithmetic used to give for free: whole pixels on both axes; within `RATIO_TOLERANCE` of the painted ratio; `checkDimensions` accepts a slot's own recommended size (the round trip — the panel must never call its own advice the wrong shape); and the stated width covers the widest render so nothing is upscaled.
- [x] 8.3 Size badge on each slot heading in the grid, so the figure is readable BEFORE a merchant opens the upload dialog. The dialog kept the fuller three-part string with the store's content width named. Reverses the earlier "no longer prints the size guidance" decision, and the comment on `SlotSection` says why: a merchant makes hero artwork before opening this panel, so a size that only appears after they have picked a file is a size they learn too late.
- [x] 8.4 Per-layout sizes listed inside each picker card, so switching can be judged against artwork the merchant already has.
- [x] 8.5 Picker dead-click fixed, two causes. `disabled={updateSettings.isPending}` read the SHARED settings mutation, so any other editor's save killed the whole control with nothing on screen saying why — replaced with a page-owned `savingVariant`, cleared in a `finally`, disabling only the card being written. And the cards did not stretch to their grid row (`h-full items-stretch`), so a card shorter than its neighbours left a band inside the cell that was not part of the button and swallowed clicks.
- [x] 8.6 `chooseVariant` returns early when the clicked layout is already selected — Radix fires `onValueChange` for the selected card too, which flashed a success toast for a no-op write.
- [x] 8.7 `variant-picker.test.tsx` pins the behaviour rather than the styling, since styling is what was wrong both times: a click reports the right layout, a save in flight leaves the other cards live, and every card prints whole-pixel sizes.
- [x] 8.8 Geometry re-verified against `nextjs/src/components/home/hero/` slot by slot — 43% column, square side tiles, 43:20 promo, 3:1 slider, 4:3 stacked tiles, three-across row, `gap-4` = 16 and `container-px` at `lg` = 64 all agree. Corrected this module's stale `frontend/` path to `nextjs/`.
- [x] 8.9 Duplicate size on an empty slot removed. The heading badge and `EmptySlot`'s caption both printed `slot.recommended`, so an empty slot showed the same figure twice, two lines apart and in two spellings — "1400 × 1400 px" against "1400 px × 1400 px". The placeholder keeps the SHAPE (`aspectRatio`, which prints no number) and drops the figure; the badge now formats via `formatSize`, so the panel spells a size exactly one way wherever it appears. `hero-slots.test.ts` guards both against the source, and the guard was confirmed to fail when the duplicate is put back.
- [x] 8.10 SPLIT_ONE not rendering in the slot grid. The side column was drawn as `xl:w-96` — a hardcoded 384px. Correct-looking for SPLIT_THREE, whose column holds two half-width square tiles; silently wrong for SPLIT_ONE, which renders ONE square tile at the full 43% column (592px at a 1440 content width), so the tile drew at two thirds of its size and the slider took the rest. The picker, the badges and the saved setting were all correct, which is why it read as "the layout doesn't show". Now `xl:w-[43%]`, matching `HERO_GEOMETRY.sideColumnFraction` and the storefront's `lg:w-[43%]`. Guarded behaviourally in `hero-slots.test.ts`: both split layouts share the fraction, the slider is the row minus column minus gap in both, and SPLIT_ONE's tile is the full column (>384px) where SPLIT_THREE's is half of it.
- [x] 8.11 One card rendering taller than the other three. `h-full` on the card was the cause: that is `height: 100%`, and a percentage height on a GRID ITEM resolves against the row height, which the items' own content is what determines — circular, and browsers resolve it inconsistently, so one card stretched while the markup for all four was byte-identical. A grid item already gets `align-self: stretch`, so it fills the row with no height declaration at all. Removed; `items-stretch` stays because it is the INNER flex column's cross-axis rule, and `mt-auto` on the size list still pins it to the card's bottom. `variant-picker.test.tsx` pins the absence of any percentage height, that all four cards share their structural classes, and that all four diagrams draw on one `viewBox` — confirmed to fail when `h-full` is put back.
- [x] 8.12 `SPLIT_ONE` WITHDRAWN, on request, across all three apps. It was the only layout whose side column held one tile at the full column width, which made it the odd one out in every surface that drew the set. Removed from the server's `HERO_VARIANTS` (writes now 400 with a message naming the three offered layouts; a store still holding it READS as the default via `resolveSectionVariant`, so nothing renders empty), the storefront's `HERO_VARIANT_KEYS`/registry/types (the `HeroSplitOne` component and skeleton deleted; `resolveHeroVariant("SPLIT_ONE")` pinned to the default), and the admin's `HeroVariant`/`HERO_VARIANT_OPTIONS`/`HERO_GEOMETRY`/`SLOT_COPY`/picker diagram. The live store is on `SPLIT_THREE`, so no store was stranded. Picker grid is now `xl:grid-cols-3`.
- [x] 8.13 All picker cards one height. The descriptions wrap to different line counts and the size list is one row for FULL_SLIDER and three for the others, so the cards ended at different heights and the selected one's background made it visible. Both blocks now reserve their maximum height (`min-h-12` / `min-h-14`) so every card matches the tallest whatever it says; `items-stretch` stated explicitly on the grid.
- [x] 8.14 EVERY LAYOUT PAINTS THE SAME OUTER BOX. Requested: the three layouts were 579, 459 and 811px tall at a 1440 content width, so switching layout moved the whole page below the hero by up to 350px. `SPLIT_THREE` is the reference — its height is its tile column, 0.415 × row + 8px — and the other layouts now fit a box of that shape rather than choosing their own. `HERO_RATIO = 19/8` (admin) ↔ `lg:aspect-19/8` (storefront `HeroFullSlider`, `HeroSliderStack`) is that column's ratio to within 4px across 1140–1920; the 8px gap keeps it from being exact and CSS `aspect-ratio` cannot express "+8px". FULL_SLIDER fills the box (2850×1200). SLIDER_STACK keeps a row of 43:20 tiles — the promo's shape, so promo artwork renders uncropped in both — along the bottom and lets its slider stretch to what is left (`lg:flex-1 lg:min-h-0`; 1376×355 @1440, upload 3100×800). `hero-slots.test.ts` pins all heights within 4px at every width, and the width-independence test now judges RELATIVE drift against the stated size by `RATIO_TOLERANCE` — its old absolute ±0.05 was 1.3% on a 3.9:1 slider and 3.7% on a 4:3 tile.
- [x] 8.15 Fourth layout `SPLIT_TALL`, on request: a wide slider (two thirds) beside one tall tile filling the right third. The tile is row/3 at 19:24, chosen because (row/3) × (24/19) = row × 8/19 — the shared box's height EXACTLY, so this is the one layout that matches SPLIT_THREE to the pixel. Reads `HERO_SLIDER` + `HERO_PROMO` (capacity 1); `HERO_SIDE` unrendered. Uploads 1560×1000 and 950×1200. Appended LAST in every registry (server `HERO_VARIANTS`, storefront `HERO_VARIANT_KEYS`/registry/`HeroSplitTall.tsx`/skeleton, admin type/options/geometry/copy/diagram) so the default stays at position 0. Portrait at every breakpoint on the storefront — a 4:3 crop on phones would discard half the artwork. The admin page now reads `stacked` and the side column's share from `HERO_GEOMETRY` (via a `--side` CSS variable) rather than naming layouts, and the picker is back to four columns.
- [x] 8.16 `SPLIT_TALL` rendered as NOTHING on the storefront while the other three worked. Traced end to end: the admin saved it, `GET /settings/public` published it, the served HTML carried every class (`aspect-19/24`, `lg:w-1/3`) and the emitted CSS had both utilities — and the hero was 0px tall. Cause: `HeroTile` renders an `<a>`, which is `display: inline` and ignores `aspect-ratio`/`width`. Every other layout places the tile directly in a grid or flex column, which blockifies it; `HeroSplitTall` wrapped it in a plain `<div>` to give it a third of the row, so it stayed inline, the column collapsed, and the slider that stretches to the column collapsed with it. Fix: the wrapper is `flex`, blockifying the tile as the others do; skeleton mirrored. `HeroSplitTall.test.ts` pins the class on both against the source (the runner cannot render `next/image`), and the live storefront was re-fetched to confirm the fixed wrapper is served.
- [ ] 8.17 Clicking the picker, reading the badges, and switching all four layouts in a browser. Still needs a human — there is no browser automation in this repo. Still needs a human — there is no browser automation in this repo.

## 7. Close out

- [x] 7.1 Geometries compared against `frontend/src/components/home/hero/` by script, slot by slot: the 43% column, the square side tiles, the 43:20 promo, the 3:1 slider, the 4:3 stacked tiles, the three-across row, and the shared gap and container padding all agree — and each file still names the other as the thing to keep in step.
- [ ] 7.2 Confirm the server and storefront changes are both DEPLOYED before this one ships. Both are implemented in this working tree and verified against a locally running stack, but nothing has been committed or deployed — that is the remaining gate (design.md — Migration Plan).
